# BUY Partial Fill Immediate Cancel Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cancel an accepted BUY GTC immediately after an official partial fill, while preserving the existing post-cancel-finality no-duplicate invariant.

**Architecture:** Extend the existing `cancel_due_active_gtd_orders` maintenance boundary rather than adding a new scheduler. The function keeps the current deadline trigger and adds one official-order trigger for `0 < matched < requested`; all reconciliation and retry behavior remains in the existing state machine.

**Tech Stack:** Python, SQLite receipts, py-clob-client-v2, pytest, systemd closed-loop release.

---

### Task 1: Lock the behavior with failing tests

**Files:**
- Modify: `tests/test_cd90_live_copy.py`

- [ ] Add a test whose GTC deadline is still in the future and whose official order reports a partial match; assert the exact order is canceled and the transition reason is `GTC_ACTIVE_CANCEL_PARTIAL_FILL_DETECTED`.
- [ ] Add controls asserting a future-deadline zero-fill order and a fully matched order are not canceled early.
- [ ] Run the three tests and confirm the partial-fill case fails because the current function checks only the deadline.

### Task 2: Add the smallest shared trigger

**Files:**
- Modify: `app/cd90_live_copy.py`

- [ ] In `cancel_due_active_gtd_orders`, parse the already-recorded requested quantity.
- [ ] Before the deadline, read the authenticated official order and classify only `0 < matched < requested` as an immediate-cancel trigger.
- [ ] Reuse `cancel_active_gtd_order`; persist `active_cancel_verified`, the trigger reason, and observed matched quantity without releasing the reservation.
- [ ] Keep deadline cancellation, zero-fill behavior, full-fill behavior, SELL, UNKNOWN, and finalized reconciliation unchanged.
- [ ] Run the focused tests until green.

### Task 3: Verify and publish

**Files:**
- Modify only release receipts generated by the existing release transaction.

- [ ] Run `python -m py_compile app/cd90_live_copy.py` and the affected GTC/retry tests.
- [ ] Run the complete related live-copy/release test set.
- [ ] Re-check the server mutation gate: no unsafe submission, UNKNOWN repost, active reservation, or authenticated open order.
- [ ] Commit only the spec, plan, test, and shared implementation; preserve the existing unrelated `AGENTS.md` modification.
- [ ] Deploy through the existing closed-loop release transaction as repair version `3.17`.
- [ ] Verify the canonical version resolver, release identity, zockdo primary/hot-standby, fresh heartbeat, cursor lag, SQLite integrity, action conservation, official open orders, reservations, and UNKNOWN submissions.
