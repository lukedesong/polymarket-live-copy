---
description: Polymarket 当前修复版本只以服务器固定索引为准，禁止用目录名或对话旧数字冒充版本。
alwaysApply: true
---

# Polymarket 版本权威

处理任何本项目运行、巡检、修复、报账或部署请求时，第一步通过 SSH 别名 `polymarket-hk` 读取 `/opt/polymarket-live/CURRENT_REPAIR_VERSION.json`。

- 唯一权威版本号是 `semantic_repair_version`。
- 必须同时验证其 `release` 等于 `/opt/polymarket-live/current` 的真实目标，且 `source_commit_receipt_sha256` 等于该 receipt 文件的实际 SHA-256。
- 发布目录时间戳、change_id、本地未提交补丁和聊天里的旧数字都不是版本号。
- 这类回复的第一行必须是 `当前版本: <semantic_repair_version>`。失败时写 `当前版本: BLOCK_VERSION_AUTHORITY`。
- 只有 `VERIFIED_FIXED` 才升版本；一次修复只把 minor 加一。
