# Polymarket 项目协作规则

## P0：只有实盘，禁止再说纸面

Luke 原句（2026-08-17）：从现在开始彻底忘记纸面这一说法，只有实盘。

- 对用户只说实盘跟单。禁止再用纸面、纸面实盘、策略纸面、开纸面、纸面毕业、纸面采集。
- 现网已接入的源钱包跟单就是实盘。新钱包研究的下一步是实盘资格和是否接入现网执行器，不是再开一套模拟账户。
- 代码里的 `paper_only` / `real_order_submitted` 只是安全旗标和审计字段，不是产品名称，不得写进对用户的结论。
- 本条覆盖下文所有仍写着「纸面」的旧段落。那些段落只保留作历史合同，不得再当当前说法或下一步动作。
- 本条不自动授权新钱包下单，也不自动改 `POLYMARKET_LIVE_TRADING`。真实下单、撤单、改 live 开关仍须点名。

## P0：当前修复版本权威

处理任何本项目运行、巡检、修复、报账或部署请求时，第一步通过 SSH 别名 `polymarket-hk` 读取 `/opt/polymarket-live/CURRENT_REPAIR_VERSION.json`。

- 唯一权威版本号是该固定索引的 `semantic_repair_version`；必须同时验证其 `release` 等于 `/opt/polymarket-live/current` 的真实目标，且 `source_commit_receipt_sha256` 等于其 `source_commit_receipt` 的实际 SHA-256。
- 发布目录时间戳、change_id、对话中的旧数字和未提交候选都不是版本号。
- 运行/巡检/修复/报账/部署回复的第一行必须是 `当前版本: <semantic_repair_version>`。读取或一致性验证失败时第一行改为 `当前版本: BLOCK_VERSION_AUTHORITY`，禁止猜测。
- 只有真正达到 `VERIFIED_FIXED` 的修复才更新版本；诊断、报告、外部限制、失败发布和单纯进程对齐不更新。每个 `VERIFIED_FIXED` 将当前 `major.minor` 的 `minor` 精确加一，且一次修复只加一次。

## P0：实盘跟单采用最小闭环，禁止再次过度工程化

Luke 已将本项目“把他买我买、他卖我卖的简单跟单需求扩张成财务、发布证明和无止境审查工程”认定为非常严重的 Badcase，要求永久避免。

- 当前实盘跟单核心只有：发现源动作、按已确认比例映射、读取官方约束并提交官方订单、用官方订单/成交/链上回执核单、保存去重与 UNKNOWN 不重发证据、只卖跟单形成的归属库存、持久化前向游标，以及共享认证钱包所必需的单一提交互斥。
- 官方 authenticated collateral、官方 order/trade 和链上 receipt 是余额、成交、费用与结算权威。本地状态不得发展成第二套钱包财务，不得因人工买卖造成的本地现金/PnL差异自行改账、补单或阻断无关跟单。
- 发布、回滚、热备、健康检查和锁只实现防止重复订单、误卖人工持仓、漏动作、数据损坏或不可恢复停机所需的最小机制。不能直接降低这些已复现风险的设计不进入当前切换范围。
- 新旧架构不得长期混用。新架构完成即时验证并取得不可变提交证据后，删除旧应用运行入口、旧 release 和旧锁；保留 live SQLite 与不可变发布/订单证据。
- 修复时先写最小失败反例并修共享根因；通过受影响完整回归和即时服务器复核后停止。禁止在已满足交付条件后继续追加理论加固或启动新一轮开放式审查。

## 操作 P0：持续运行纸面实盘

用户明确指定：本项目的首要任务是**持续运行真实当下市场中的纸面跟单**。这表示
源钱包有新 BUY/SELL 时，按既定纸面规则持续记录成交、部分成交、跳过、卖出、资金
占用和官方结算，并保留可复算的执行证据；它不是离线回测、状态页展示或只做钱包研究。

- 共享执行闸门、研究不足、毕业未达标、历史错误或暂时没有新动作，只影响证据分类、
  毕业和真实订单资格，**不得停止已确认账户的纸面采集、源动作跟随、卖出或结算**。
- “无成交”只能表示本轮观察到的源钱包新动作数为 `N=0`（实证值），不能被包装成
  已暂停、已完成或无需运行。运行状态必须核验进程、心跳、游标、回执守恒、SQLite、
  页面和 `paper_only=true` / `real_order_submitted=false`。
- 钱包研究、纯执行闸门、策略毕业和最小真实 canary 都是为纸面实盘服务的下游工作，
  不得反过来占用或替代这个操作 P0。
- 用户指定的三小时巡检是 P0 的维护闭环：安全可重试的内部错误自动修复并复核；
  不能安全自动修复的账务、回执或安全问题保持不可变并升级；巡检不能只生成报告。
- 巡检必须先按可控性分流：代码、服务、游标、账务、元数据、页面或我方流程导致的
  内部问题，先写失败测试、修复共享同类路径、完整复验并恢复采集；网络、RPC、CLOB、
  Data API、市场关闭或官方结算接口等外部限制，保留不可变发生时间、端点/通道、
  精确源动作绑定（若有）和处理终态，不能被“重启成功”覆盖。
- 外部因素进入复制经济性时不得猜测一个损失率：已成功复制动作的价格、费用和发现
  延迟损耗只在 `copy_loss_interval` 中计一次；某个精确源动作因外部不可得而没有冻结
  盘口/约束时，记录为未定价外部动作影响并阻断正期望/canary 资格；只有可复算的外部
  增量损耗区间，才可从剩余优势区间扣除并判断扣除后下界是否仍为正。
- 所有路径始终保持纸面模式；真实订单仍必须得到新的明确授权。

## P0：所有结果必须量化、可复算、带分母

本条与全局 P0 数字审计一体适用，覆盖所有研究、钱包筛选、
策略结论、运行状态、纸面账本、执行验收、毕业判断和用户可见报告。
不得用“健康、正常、有效、较好、很多、很少、已验证、可实盘”这类
没有量化口径的词代替结果。

- 每个结果至少同时给出：**对象、统计单位、数值、分子/分母或样本量、
  时间区间、数据来源和数字来源分类**。例如“动作回执 17 / 17，观察窗口
  为某起止时间，实证值，来源为不可变 action receipt”；不能只写“无漏单”。
- 交易行、fill、链上交易、逻辑动作、独立决策单元、独立事件、仓位和结算
  必须分开计量；不得把成交碎片数量冒充独立样本量。
- 任何“健康”结论必须分别量化：进程数、心跳年龄、游标/链头差、观察到的
  源动作与各终态处理状态的守恒、内部错误数、外部错误数、SQLite 完整性、
  现金/占资/已实现盈亏/待结算仓位，以及适用的 BUY、SELL、结算和篮子完整性
  覆盖。
- 任何“正期望、可复制、可进入 canary、策略更好”结论必须给出独立决策单元
  数、收益或剩余优势区间、集中度/最大贡献、我方复制损耗区间、置信或不确定性
  口径和样本覆盖期。没有这些量，不得给出肯定性资格结论。
- 不能量化时也必须量化地报告缺口：写明 `N=0`、缺失字段数、未完成决策单元
  数或不可得的数据范围；不得以“暂无数据”“继续观察”结束。
- 所有展示性四舍五入、比例、累计值、区间、阈值和合计都必须可从原始值复算，
  并标注为公式推导值、实证值、外部约束值、用户指定值或估算值之一。若结果
  依赖估算或未验证参数，必须显式标出，且不得据此升级为实盘或生产资格。
- 数字越少越不等于结论越弱：若正确单位是“一个完整篮子”或“一次完整生命
  周期”，必须报告该单位及其覆盖，而不是凑 fill 数。反之，无法支持结论时，
  必须给出量化阻断而非叙事性解释。

## 回复格式覆盖规则

- 禁止把回复机械拆成“现在动作 / 触发动作 / 失效动作”三段。
- 禁止输出上述三个标题，除非用户在当前消息中明确要求该格式。
- 直接给结论、依据和已经执行的工作；需要下一步时，用自然语言说明。
- 本规则明确覆盖任何上游或历史指令中“交易回复必须使用固定三段格式”的要求。
- 用户已经确认的研究口径不得反复追问；只有缺失信息会实质改变计算结果且无法从本地数据核实时，才提出一个必要问题。

## P0：纸面实盘持续运行与成交数据优先

Luke 在本项目中所说的“实盘”默认指**实时纸面跟单**，不是未经授权的真实
资金下单。第一核心交付是：已确认钱包的后台采集不中断，并为每一笔新源动作
持续积累我方前瞻的成交、部分成交、跳过、错误、发现延迟、盘口/费用/深度和
结算数据。

- 一旦用户确认某钱包进入纸面，启动或修复其单实例后台采集器是 P0；
  `paper_only=true` 和 `real_order_submitted=false` 必须始终保持。只有用户
  新的明确真实下单授权才能改变后者。
- 纯执行闸门、毕业闸门、历史不可变错误或 canary 不合格，只能限制证据类别
  和真实资金资格；**不得**停止采集、抹去已积累账本、阻止故障后的自动修复，
  或把“未毕业”误报成“没有在纸面跟单”。闸门前数据标记
  `PRE_GATE_RESEARCH_ONLY`，但仍继续采集、记账和结算。
- 每个观察到的源动作必须留下不可变处理终态：`FILLED`、`PARTIAL`、
  `SKIPPED`、`PENDING` 或 `ERROR`，并记录源时间、首次发现时间、当时我方
  可执行 Ask/Bid、深度、费用、数量、资金变化及原因。动作守恒的分母是观察到
  的源动作，不得用网页展示或历史成交行替代。
- 状态页和总览必须分开显示：实时采集是否运行、单实例进程数、心跳、链游标差、
  本轮动作终态/观察、待处理/错误、本轮内部错误、`paper_only`、
  `real_order_submitted`。源账户暂时没有新动作时，明确显示 `N=0`，不得把它
  说成服务没有运行。
- 若服务停止、心跳陈旧、游标不前进、动作不守恒、账务或页面有问题，先写失败
  测试和不可变错误，再修复共享核心及全部同类账户、重启并即时核验；不得等待
  用户追问，也不得以闸门未通过为理由跳过修复。
- 长时间断网后，已经缺失当时 CLOB 盘口的旧链区块不得拿当前盘口补写成成交。
  保留旧账本、仓位和结算；把不可重放区间作为不可变外部数据缺口记录，并建立
  当前链头水位后立即恢复**之后**的新动作纸面跟随。不得为了慢速回放而让新的
  实时跟单继续停摆。

本 P0 不把纸面数据冒充真实订单成交，也不把持续运行本身升级为策略正期望或
canary 资格；它确保用户始终拿得到进行这些判断所必需的前瞻成交数据。

## 当前 E2 研究边界

- E2 只做纸面研究，不接入实盘。
- 六月回测使用六月之前的数据训练概率模型，并在六月冻结。
- 单账户初始纸面资金沿用用户此前指定的 100 美元；该数值是用户指定值，不代表已验证最优。
- 历史成本使用决策时点之前最近的同方向 Polymarket taker BUY 成交价作为 Ask 代理。
- 不混入 FOK、API 延迟、盘口深度、资金回收或其它实盘执行变量。

## 灾难级 Badcase：纸面目的、执行前置闸门与毕业合同

2026-07-25 的钱包跟单流程被用户明确判定为**灾难级错误**：按用户陈述，策略纸面在最基础的执行链尚未验收、也没有预先写明何时结束和如何进入真实 canary 的情况下运行了约一天；出现问题后又用“纸面不能直接实盘”作为结论终点。该错误属于我方主观流程失败，不能归因于公开 API 延迟、市场价格变化或其它客观约束。

### 四类证据必须分开

- **源策略证据**：钱包公开历史回答“源账户的专家袖套是否可能有 edge”。
- **纯执行验收**：回答“链上发现、方向/资产解码、盘口读取、游标、去重、账本、服务和错误记录能否工作”，不评价盈亏。
- **策略纸面**：只回答钱包历史没有的数据——我方发现后的可执行 Ask/Bid、深度、费用、部分成交、跳过、资金占用、卖出和结算后，源 edge 是否仍可复制。
- **最小真实 canary**：回答纸面永远无法回答的真实签名、订单接受/拒绝、排队、真实部分成交、撤单、费用和资金结算。它仍不是放大仓位。

禁止用任意一层替代另一层；尤其禁止用源钱包盈利替代我方复制经济性，也禁止用 `paper_only=true` 这句安全声明逃避给出毕业结论。

### 纯执行必须先验收

- 任何新的策略纸面在开始累计可复制性证据前，必须先通过独立纯执行验收；未通过时策略纸面不得启动或不得计入毕业证据。
- 本项目纯执行验收窗口固定为 **30 分钟（用户指定值）**，不等待样本累计。计时只在最终活跃源已选定、真实 daemon 已为 running、`current_head == last_processed_block`、单进程计数已经写入，且起止时间已写入状态页后开始；准备和补水位时间不计入窗口。
- 每次独立验收从启动时当前链头建立新水位，只观察之后的新动作。旧数据库保留审计，但停机期间的旧事件不进入新窗口，也不得用“当前盘口”伪装成旧事件的历史盘口。
- 到点必须输出结果，不得延期：`PASS_PRIMARY_EXECUTION`、`FAIL_INTERNAL_EXECUTION`、`DEGRADED_EXTERNAL_RECONCILIATION` 或 `INCONCLUSIVE_SOURCE_SELECTION`。没有事件说明选源失败，不是继续等数据的理由。
- 闸门是否可进入下一步看 `primary_execution_passed`，不是只看字符串：`PASS_PRIMARY_EXECUTION` 直接通过；`DEGRADED_EXTERNAL_RECONCILIATION` 仅在主链发现、链时点盘口、游标、账本和无积压均通过时视为“主路径通过、外部对账降级”，后续不得再把 Data API 当计时源；`FAIL_INTERNAL_EXECUTION` 与 `INCONCLUSIVE_SOURCE_SELECTION` 不通过。
- 主观失败包括：进程/心跳/游标/区块积压、错误未记录、动作静默丢失、方向或资产解码错误、盘口读取/去重/账务错误、重复进程。客观限制包括已测得的公共接口展示延迟、盘口变化和深度不足；客观限制必须量化并路由处理，不能掩盖先验收缺失。
- 公共 Data API 对账的 `404`、公共 CLOB/RPC 传输错误与内部吞吐/账务错误必须分栏；外部失败可以形成 `DEGRADED_EXTERNAL_RECONCILIATION`，不能被错误合并成内部 bug，也不能反过来掩盖区块积压。
- 每个运行异常必须先追加写入不可被“恢复成功”覆盖的 SQLite 错误事件（时间、通道、主观/客观类别、异常类型和原文），再更新页面状态。只有累计计数或会被清空的 `last_error` 不构成可审计证据。
- 探针是有界任务：截止线程在固定截止点冻结结果并正常停止，LaunchAgent 不得在正常截止后重启。只允许异常退出时重启以保证仍能产出失败回执；异常重启必须增加进程计数并使窗口不能通过。到点后继续累计或正常退出后自动拉起，一律视为内部验收失败。
- 纯执行通过只证明执行观测链可工作，不证明策略正期望；但它必须在策略纸面之前完成，不能等纸面亏损或漏单一天后才补做。

### 每个纸面上线前必须有目的与出口

纸面上线回执必须在启动前写明：

1. 钱包历史已经回答了什么；
2. 纸面唯一新增、且会改变决策的数据是什么；
3. 使用哪个源动作单位和我方可执行价格；
4. 最迟何时给结论；若依赖事件结算，写明具体事件或最晚结算节点；
5. PASS、FAIL 和缺数据各自触发什么动作；
6. 通过后最小真实 canary 的市场选择方法、当前外部最低订单约束、限价、最大损失公式和停止条件。

缺少任一项时禁止启动“先跑着看”的纸面账户。不得用通用的几天、几个月或任意成交笔数代替策略专属的停止规则；任何承重阈值仍受 P0 数字审计约束。

### 纸面必须毕业，不能无限观察

- 纸面结论只能落入：
  - `REJECT_NOT_COPYABLE`：我方延迟后净经济性或完整动作链失败；
  - `CONTINUE_TO_NAMED_EVIDENCE`：只允许等待启动前已写明的具体事件/结算节点和硬截止；
  - `QUALIFIED_FOR_MINIMUM_LIVE_CANARY`：源策略研究合格、纯执行通过、纸面新增的复制损耗证据未否决 edge；
  - `BLOCK_UNAVAILABLE_EVIDENCE`：关键数据客观不可得，并明确因此无法支持哪项决定。
- 达到 `QUALIFIED_FOR_MINIMUM_LIVE_CANARY` 后，必须主动给出基于当时盘口和市场最低量的 canary 提案，并只为真实资金授权向用户确认。不得继续用“纸面不能证明真实成交”把结论退回无限纸面；正因为纸面无法证明，下一实验才必须是最小真实 canary。
- 未获用户明确真实下单授权时仍禁止提交订单；这是权限边界，不是研究结论。正确表达是“已经/尚未具备 canary 资格”，而不是泛泛地说“纸面不能进入实盘”。
- 放大仓位必须等待真实 canary 的实际订单接受、成交、费用、排队/部分成交、撤单和结算证据；纸面或最小 canary 都不能自动授权放大。

### 本次旧数据降级

- 在上述纯执行闸门和毕业合同建立之前产生的 weather、Tian-Wen 及同类纸面记录，一律标记为 `PRE_GATE_RESEARCH_ONLY`。
- 它们可以用于检查市场分类、账务显示和行为假设，但不得计入 `COPYABLE_EVIDENCE`、不得证明实盘就绪、不得作为已经等待一天所以应继续等待的理由。
- 后续若继续这些策略，先取得新的 30 分钟纯执行结论，再为每个策略分别写纸面目的和毕业回执；沉没时间不进入未来决定。

## 钱包研究与纸面跟单不可再犯规则

以下规则来自本项目完整对话复盘，详细证据见 `docs/2026-07-25-wallet-research-conversation-audit.md`。规则覆盖钱包发现、策略研究、资金计算、纸面运行、状态页和交付。与旧报告、旧设计或旧代码冲突时，以本节和时间更新的明确用户要求为准。历史报告可以保留为当时证据，但不得继续充当当前规则。

### 总目标与三层证据

- 真正目标是提高我方延迟后、按可执行价格实现的未来净收益，不是复制源账户的交易外形。
- 永远分开三层：
  - **观察事实**：公开成交、仓位、结算、时间和盘口实际显示了什么；
  - **机制假设**：止盈止损、做市、套利、专家判断、Agent 或人工下注等解释；
  - **我方经济性**：我方延迟后的 Ask/Bid、深度、费用、部分成交、最低量、现金占用和结算下是否仍为正期望。
- 三层不得互相替代。看见双边成交不等于证明做市；源账户盈利不等于我方可复制；纸面代码通过单测也不等于策略经济性已成立。

### 固定研究顺序

- 顺序必须是：地址与数据覆盖核验 → 专家袖套切分 → 生命周期与策略阶段 → 源策略经济性与集中度 → 复制机制和执行风险 → 固定比例与资金重放 → 用户确认纸面 → 30 分钟纯执行闸门 → 写明纸面目的与毕业回执 → 策略纸面生产验收 → 毕业结论。
- 未完成账户研究回执前，不创建正式纸面运行时。用户已经明确确认纸面后，则直接执行后续流程，不再重复确认已知信息。
- 源钱包历史、排行榜、回测、单测和纸面服务健康均不能单独授权实盘；它们只能按本文件的毕业合同支持或否决 `QUALIFIED_FOR_MINIMUM_LIVE_CANARY`。任何真实订单仍需用户新的明确授权。

### 自主判断，禁止机械遵从

- 用户给出的领域占比、成交笔数、观察时长、资金和阈值首先视为例子、假设或用户指定值，不自动升级为普适规则。
- **“用户指定值”只是来源标签，不是合理性证明，也不是免除盲从审计的通行证。**
- 使用任何用户指定值前，必须先判定当前处于哪个阶段：
  - **研究阶段**：用户指定值默认是待检验假设、情景输入或偏好线索。必须检查机制、替代解释、敏感性和数据证据；不得直接控制策略分类、候选排序、晋级、淘汰、正期望或理论资金结论。
  - **执行阶段**：只有研究结论和承重参数已独立通过验证后，用户指定值才可作为预算、范围、纸面模式、账户隔离或其它操作约束执行；它仍不代表最优或已验证合理。在该约束内部，比例、分配、时序和执行方案仍必须继续优化。
- 同一个数字若同时承担“用户偏好”和“研究前提”，必须拆开输出：按用户偏好的情景结果，以及基于证据的独立判断。不得把前者冒充后者。
- 研究阶段无法验证用户指定值时，保留为连续指标、做敏感性分析或标记 `BLOCK_DATA`；禁止为了推进任务而硬编码成门槛。
- **不能以“用户指定值不代表最优”作为结论终点。** 对能够由目标、数据和约束决定的参数，必须主动搜索证据支持的最优可行值，而不是保留默认值或把选择退回用户。
- 优化前必须明确目标函数、约束、候选集合、验证区间和失败条件。本项目默认优化未来可实现净收益，同时受真实执行、资金占用和已确认风险约束；不得把样本内最好冒充全局最优。
- 只有属于用户效用、风险承受、授权或外部资源边界，且客观无法从市场数据推导的变量，才由用户决定，例如总投入金额、最大可接受损失、必须保留的流动性和可投入时间。即便这些边界由用户给出，边界内方案仍由我寻找最优。
- 数据不足、环境非平稳或多个目标互相冲突而无法识别唯一最优时，必须交付当前证据下的最优可行候选、有效前沿或稳健区间，并明确缺什么证据会改变选择。禁止用“无法最优”掩盖未搜索、未比较或未做敏感性分析。
- 用户提出机制时，必须同时给出：成立部分、尚未证明部分、替代解释、可证伪测试。不得因为想法有启发性就包装成事实。
- 被用户纠正后不得从一个极端摆到相反极端。应重建分层证据：行为描述 → 历史风险 → 低成本验证 → 前瞻纸面 → 复制结论。
- 能由我根据数据、目标和成本判断的事项，由我直接判断。只有风险偏好、实盘授权或无法从数据恢复且会实质改变结果的选择才问用户。
- 内部流程、skill 或工程偏好不能变成用户反复确认的手续。清楚授权下的只读分析、纸面工作和可逆代码修改直接做完。

### 动态发现与候选交付

- 旧钱包名单只是种子和 provenance，不是搜索宇宙，不得反复只扫描固定旧名单。
- 使用可续接的动态候选池、地址去重、增量检查点、来源覆盖和失败记录；排行榜 PnL 只用于发现，不用于证明优秀。
- 严格区分“发现线索注册表”和“活跃候选池”：尚未轮到筛查的地址只是线索，不能因运行批次上限而判负；完成筛查却没有晋级的地址必须退出活跃队列并保留淘汰证据。只有写明可核验的强理由和重新进入条件，才可留在例外复审或公式研究队列。
- 每个候选一次性交付：昵称、完整钱包地址、可点击 Polymarket 页面、专家袖套、数据覆盖期、生命周期、策略阶段、源经济性证据、复制风险、下一验证动作和当前结论。
- 搜索任务的完成标准是实际运行搜索并交付新候选分析，不是只写扫描代码、设计文档或计划。

### 数据覆盖与统计单位

- 每份钱包报告开头必须给 coverage manifest：端点、最早/最晚时间、原始行数、唯一交易哈希、独立事件、分页方式、是否截断、开放仓与结算覆盖、未知项。
- API 返回上限、行数或最近页不能代替时间覆盖。无法证明完整时，禁止写“所有交易”“完整盈亏”“完整周期”。
- 公开数据必须区分：fill 行、链上交易、逻辑动作、订单、策略决策、事件、仓位、结算、挂单/撤单。没有源订单 ID 时，不得把最小 fill 当成完整订单。
- 频次必须同时说明统计单位和分母：fill、唯一交易、逻辑动作、事件；按日历日还是活跃日；使用哪个时区。
- BUY/SELL 只描述该钱包相对 outcome 的交易方向，不自动证明 maker/taker。缺独立角色证据时写“角色未知”。

### 专家袖套与正期望

- 先用不含结果泄漏的领域和行为规则切分专家袖套，再看盈亏。不得看见某领域亏损后才把它改称“乱赌”。
- 钱包历史只能叫观察性实盘轨迹，不得叫我方真实回测。成交行数不是独立样本量。
- 有效证据以独立事件、策略决策簇和时间阶段为主；必须检查收益集中度、最大事件贡献、去掉最大事件后的结果、连续亏损、早晚阶段、行为变点和当前阶段样本。
- 发现期、策略定义期和验证期必须尽量时间隔离。不能从盈利排行榜找赢家，再用同一段历史证明赢家优秀而不披露选择偏差。
- 胜率和累计 PnL 不能单独证明正期望；必须结合入场价、赔率分层、费用、事件级收益分布、阶段稳定性和样本外证据。

### 生命周期、做市、高频与篮子

- 持有到结算、中途卖出、止盈止损、反转、对冲、篮子和库存循环首先都是策略描述，不是自动排除条件。
- 持有到结算也不代表跟随价格只影响“赚多少”。二元合约的我方成交价可以把同方向的正 edge 变成负 edge。
- 确认做市需要持续双边报价、挂撤、库存回归和价差收益证据；确认套利需要完整多腿锁定收益证据。只有成交形态时只能写“观察到相关迹象”。
- 用户提出的“一分钟内多次买卖”属于用户指定观察直觉，不是已验证高频阈值。时间窗只报告连续指标；真正承重的是同 token 反向间隔、机会价格衰减、我方端到端延迟和动作漏失。
- 做市或套利不是身份性禁区，但先验复制难度较高：做市必须计入我方跨 Ask/Bid 与逆向选择；套利必须整组腿同时可执行。
- 若收益依赖多选项或多市场组合，必须按 basket 原子跟踪；任一关键腿缺失都算组合复制失败，不能只统计成功成交的腿。
- 历史风险可以降低研究优先级，但不能单靠标签淘汰；也不能把“不得误杀”误解成所有钱包都值得昂贵深挖。每个候选必须说明策略类型、复制风险、下一证据成本和研究优先级。

### 固定比例、最低量与资金

- 回答资金问题前先锁定被求量和单位，禁止用相近概念代答。必须分开：
  - 源账户可观察袖套的历史资金下界；
  - 我方最小固定缩放比例；
  - 历史峰值并发占资；
  - 运营缓冲；
  - 用户指定纸面资金。
- 累计买入额不是资金池。资金下界必须按时间重放买入、卖出、费用、结算、并发持仓和实际资金释放，取最大累计现金缺口。
- 市场最低订单量按具体市场、时间和订单类型现场读取，属于外部约束；不得把某市场的最低量推广成平台永久统一规则。
- 极小公开成交可能是部分 fill 或展示碎片。先按可验证边界合并逻辑动作；无法恢复动作边界时输出 `BLOCK_ACTION_RECONSTRUCTION`，不得据此生成精确比例或实盘资金。
- 比例复制必须保持固定 share-scale 或可观察袖套权重；不知道源账户总权益时，不得称为同比风险。
- 最低量只决定成交、部分成交或明确跳过。禁止向上凑整放大仓位，也禁止把多个过期信号攒到最低量后再按新价格成交。
- 用户指定纸面现金只是缓冲，不反向改变固定比例，也不得被当成其它账户的默认资金。

### 前瞻复制与纸面经济性

- 源策略历史可以证明研究价值或暴露负面证据，但不能证明我方复制收益。
- 前瞻纸面必须使用发现后可执行的 Ask/Bid、可见深度、费用、部分成交、最低量、现金和结算；所有未成交与跳过都保留。
- 同时报告已成交样本收益和 intention-to-follow 收益，防止只挑成功成交的动作计算 PnL。
- 每个源 SELL 必须映射我方已有库存；水位前未回填仓位、此前漏掉 BUY 或库存不足时，明确跳过并计入动作链复制失败。
- 前瞻纸面本身仍可能乐观。它可以按毕业合同给出 `QUALIFIED_FOR_MINIMUM_LIVE_CANARY`，但不能自动提交订单或放大仓位；真实订单必须经过用户新的明确授权，并用最小真实 canary 校准。

### 纸面运行与生产验收

- “脚本写了、单测通过、HTML 生成”都不等于“已上线/正在实时运行”。
- 上线必须验证完整生产链：OS 调度服务 → 公共数据请求 → 游标/水位 → 决策 → SQLite 原子落账 → status.json → 页面自动刷新与陈旧检测。
- 每个源动作先写不可变 action receipt，并且只能落入：成交、部分成交、明确跳过、待处理、错误。必须满足“观察到的源动作 = 各处理状态合计”，不允许异常让动作静默消失。
- 成交流必须分页追到已处理边界；找不到边界时 `BLOCK_CURSOR_GAP`，不得依赖“轮询很快所以不会翻页”的假设。
- 净仓快照和动作复制是两种不同模式。评估止盈止损能否跟住时必须有成交流；净仓快照只能用于对账和修正，不能证明中间买卖已捕获。
- 轮询间隔不等于复制延迟。必须记录源时间、API 首次可见、本机发现、盘口快照、纸面记账时间，并按样本报告端到端延迟。
- 浏览器自动刷新不等于后台服务健康。状态页必须根据当前时间识别心跳过期，daemon 停止后页面自动显示陈旧/故障。
- 用户要求“每天/持续/定时”时，必须交付真实 OS 调度、持久状态、失败记录、上次/下次运行信息；不能依赖再次唤醒 Codex。
- 修复解释器、代理、启动器、心跳、游标、账务或页面问题时，对所有同类纸面服务执行同一检查矩阵，禁止只修用户点名的一个。
- 对用户指定每三小时的纸面巡检，不能只生成报告：服务、进程、心跳、链游标、积压、待元数据/盘口和待动作等可重试流水线错误，必须用该账户的精确 LaunchAgent 重启一次，再以新快照复核进程、心跳、链头/游标、动作回执守恒、SQLite 与 `paper_only=true`；启动命令成功不等于修复。回执不守恒、游标缺口、身份、账务、SQLite、配置或安全旗标错误不得靠重启或重建账本掩盖，必须保留不可变原始证据、追加不可变巡检升级记录、在 JSON/Markdown/HTML 显示 `CODE_REPAIR_REQUIRED_NO_LEDGER_REWRITE`，并使本轮巡检失败。公共 RPC/CLOB/Data API 限制单列为外部重试，不得重启健康账户冒充修复。
- 停止与删除分开验收：服务卸载、进程不存在、心跳停止；安装 plist、入口和自动重启路径清理；账本按用户授权归档或删除。

### 状态页固定合同

- 首屏必须能直接看懂：可用现金、已实现盈亏、未实现盈亏或“未知”、占用成本、当前清算价值、总权益、最后心跳、最后成功拉取、最后源动作、最后纸面决策、最后错误和服务健康。
- 持仓按持仓中、待结算、已结束分组；待结算继续占资，不提前算作真实已实现盈亏。
- 每笔卖出或结算显示成本、回款、费用和单笔盈亏；单笔合计必须与账户累计值核对。
- 市场结束时间、实际结算时间和资金释放时间分开；用户界面时间统一显示上海时间，同时保留原始时间与来源。
- 纯展示修改不得重建或改写账本。修改前后必须核对动作标识、账本行数、现金、持仓和累计盈亏不变。

### Polymarket 唯一账务口径

- 任何“持仓、现价、余额、总资产、已结算盈亏、未结算盈亏、报账”问题，先完整读取并遵守
  `docs/2026-08-11-polymarket-accounting-canonical-contract.md`。
- 第一行必须写明对象：源钱包公开账、我方真实共享钱包或我方策略 sleeve 账；三者禁止混算。
- “持仓”默认先列官方 `/positions` 的全部正数量记录，再分可交易、已关闭待结果、可赎回正价值、
  已结束零价值。禁止用 `redeemable=false` 静默过滤。
- “现价”默认同时区分页面盯市价与立即成交的 Bid/Ask；全仓退出必须逐档计深度和费用。已关闭仓位
  没有可执行现价，只报告官方兑付状态和价值。
- “余额/可用金额”对我方账户只指官方鉴权抵押现金减活动真实 BUY 预留。sleeve 的
  `strategy_attribution_cash_usd` 是内部归属账，`allocation_usd` 是固定比例输入，二者都不是第二个真实余额。
- “总资产”对我方账户默认指真实现金加官方仓位盯市价值；对源钱包因公开现金流不可见，只能称
  “公开仓位价值”。“已结算盈亏”不得混入当前持仓、可赎回但尚未入账价值或页面浮盈亏。
- 官方总账、逐行仓位和内部归属账不能闭合时，必须报告快照时间、差额和缺口，禁止挑一个数字或造补差数。

### 沟通、交付与完成声明

- 先回答用户真正问的对象和单位：地址、网页、金额、股数、资金池、成交状态或运行状态；再补必要解释。
- 内部枚举和技术词必须同时给中文含义、为何重要和对跟单的影响。不得要求用户理解 `FORMULA_RESEARCH`、`ACTIVE_EXIT`、`SPEED_REVIEW` 等代码语言。
- 用户确认过的范围不重复问。用户授权“自行判断”后，给出我的判断、依据和失败条件，并完成可逆工作。
- 简单任务采用最小充分实现，但不得省略数据正确性、账务、游标和生产验收。内部流程复杂度不能转嫁给用户。
- 长任务更新写清已完成、正在做和剩余交付；不能把设计、计划、代码或进程启动中的某一步称为整个任务完成。
- 声称“有成交、会记账、实时运行、已修复、测试通过、已完成”前必须取得本轮即时证据，并给用户可自行核验的页面、状态、时间或命令结果。
- 每次用户纠正都先升维到同类问题：更新通用合同、搜索旧规则、修改所有同类实例、添加反例测试。禁止只修点名账户或单个页面。

### 回复前强制检查

在任何钱包结论、资金结论、运行状态或完成声明前，内部逐项检查：

- 是否打开了 Obsidian `纠错回放卡` 并对匹配类打了 PASS/FAIL？Inbox 里已有同类纠错却未执行，不得给出推荐。
- 盈亏是否同时用了官方已结算和未平仓盯市加 `/value`，而不是只用 `/closed-positions`？CSV/排行榜/活动页是否被当成了账？筛选是否按 `docs/新钱包筛选标准.md` 先源后复制？
- 当前是研究阶段还是执行阶段？用户指定值正在充当情景约束，还是被我偷换成了研究证据？
- 我是否停在“这不是最优”，却没有继续搜索最优可行值？若声称客观无法确定，是否已说明不可识别的原因、有效前沿和缺失证据？
- 我回答的是用户真正问的量和单位吗？
- 数据覆盖、分页和未知项是否明示？
- fill、交易、逻辑动作、事件和策略是否分层？
- 这是观察事实、机制假设，还是我方经济性？是否混用了？
- 用户例子是否被我机械硬化成阈值？
- 是否检查了反例、收益集中、策略换挡和选择偏差？
- 是否使用了我方延迟后可执行价格，而不是源成交价？
- 比例与资金的上游输入是否已证明，结算占资是否计入？
- 若声称运行，真实 daemon、心跳、游标、账本和页面是否全部通过？
- 若用户纠正了一个例子，我是否已经修复整个同类，而不是只修这个点？
